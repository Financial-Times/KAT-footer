// import {PropTypes} from 'react';

const defaultState = {
  theme : "theme-light",
  footerType: "short"
};

const katFooter = (state = defaultState, action={}) => {
  return state;
};

export default katFooter;