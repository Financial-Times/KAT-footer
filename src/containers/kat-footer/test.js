import React from 'react';
import { expect } from 'chai';
import { mount } from 'enzyme';
import KatFooterContainer from './index.js';

describe('KatFooterContainer', () => {

  const wrapper = mount(<KatFooterContainer />);
});
